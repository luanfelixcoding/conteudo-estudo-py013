"""
Dada uma tupla com coordenadas 2D (x, y), 
calcule a distância de cada ponto e a distância total percorrida, 
assumindo que cada ponto é visitado em ordem.

DICA: Use a biblioteca 'math' para calcular a raiz quadrada.

Tupla de coordenadas
caminho = ((0, 0), (3, 4), (6, 8), (6, 10))

Fórmula da distância entre dois pontos:
√((x2 - x1)² + (y2 - y1)²)

OUTPUT:
Distância entre (0, 0) e (3, 4): 5.00
Distância entre (3, 4) e (6, 8): 5.00
Distância entre (6, 8) e (6, 10): 2.00
Distância total percorrida: 12.00
"""
